rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default Deny All (Tahap A)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
